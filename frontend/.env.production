# Baked into the bundle at BUILD time by Vite. Vercel injects any environment
# variable named VITE_* set in the project settings, which overrides this file.
VITE_API_BASE_URL=https://aedt-api.onrender.com
