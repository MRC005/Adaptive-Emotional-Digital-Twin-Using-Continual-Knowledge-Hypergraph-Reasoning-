// Point the demo at a deployed AEDT API. Leave empty for a purely static
// build: the page stays fully functional and the live-computation panel
// simply stays hidden.
window.AEDT_API_URL = "https://aedt-api.onrender.com";
