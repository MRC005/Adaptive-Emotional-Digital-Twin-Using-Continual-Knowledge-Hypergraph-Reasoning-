# Frozen software list (ROUND-17 §S.13). statsmodels is deliberately excluded:
# the ordinal probit is implemented directly in aedt/models/ordinal.py.
numpy>=1.26
scipy>=1.11
pandas>=2.1
matplotlib>=3.8
pyyaml>=6.0
pytest>=7.4

# ---------------------------------------------------------------- Layer 1
# Optional. Without these the emotion detector falls back to a word-list
# BASELINE and says so, and the HGNN / EWC experiments refuse to run rather
# than estimating anything. Layer 2 (the scientific core) does not need them.
torch>=2.0
transformers>=4.40
scikit-learn>=1.3
datasets>=2.18
